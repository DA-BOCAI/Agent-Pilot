package com.hay.config;

import org.springframework.context.annotation.Configuration;

/**
 * 文件作用：大模型配置保留位。
 * 项目角色：当前规划链路已切换为显式 HTTP 调用，本类暂不创建 LangChain4j Bean，避免空配置干扰启动。
 */
@Configuration
public class LLMConfig {
}
